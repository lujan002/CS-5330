# Offline card-matching pipeline. The C++ app needs none of this at runtime,
# only the files under data/card_match/inference/ these scripts produce.
#
# Install torch/torchvision from https://pytorch.org for your CUDA version
# first; the pins below are only lower bounds that were verified to work.
torch>=2.1
torchvision>=0.16
numpy>=1.24
opencv-python>=4.8
requests>=2.28
onnx>=1.15
onnxruntime>=1.16
